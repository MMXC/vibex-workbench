# ── 真实 Agent SSE（工作台闭环：不要用 mock）────────────────────
# 前置：复制本文件为 frontend/.env（或 export 同名变量）。
#
# 1. 勿开启 mock：不要设置 VITE_MOCK_SSE=1（缺省即为真实 Agent）。
# 2. Agent：在仓库 agent/ 配置 .env（OPENAI_API_KEY、WORKSPACE_DIR=本仓库根路径等），然后：
#      cd agent && go run ./cmd/web
#    默认监听 :33338；SSE = GET /api/sse/<threadId>，对话 = POST /api/chat { threadId, input }。
# 3. 前端：VITE_SSE_URL 指向同一端口（见下）。
# 4. 浏览器打开 /workbench → 选/建线程 → Composer 发送 → 应收到 SSE（run/tool/message 等）。
#
VITE_SSE_URL=http://localhost:33338
#
# ── 仅调试 mock（backend/main.go :33335）时使用下面两行，并与上面互斥 ──
# VITE_MOCK_SSE=1
# VITE_SSE_URL=http://localhost:33335
