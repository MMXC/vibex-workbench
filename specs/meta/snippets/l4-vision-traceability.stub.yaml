# ---------------------------------------------------------------------------
# Copy-paste fragment for new L4 feature YAML (merge under content:, after description).
# This file intentionally has NO top-level `spec:` — validate_specs skips unnamed docs.
# Truth for required keys: specs/meta/spec-layer-contract.yaml + VISION-TRACEABILITY-ROLLOUT.md
# ---------------------------------------------------------------------------

vision_traceability:
  summary: "（一句话：本能力如何把 L1 关切落到可验收边界）"
  l1:
    goal_path: "specs/project-goal/vibex-workbench-goal.yaml"
    constraint_ids:
      # 从 goal constraints 选 3–5 条，勿堆砌
      - "C1"
      - "C7"
    goal_sections_hint:
      - "content.constraints"
      - "content.product_value_layers（按需）"
  l2:
    skeleton_path: "specs/architecture/vibex-workbench-skeleton.yaml"
    module_id: "MOD-example"
    anchors_hint:
      - "content.modules 中 MOD-example"
      - "content.upstream_l1"
  l3:
    module_spec_path: "specs/module/MOD-example_module.yaml"
  acceptance_summary:
    - "（两条可测或可对评审口述的验收句）"
