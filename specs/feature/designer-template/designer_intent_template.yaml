# ============================================================
# Designer Intent Template — Goal/Intent Spec 生成模板
# 用途: spec_designer handler 读取此文件作为 intent spec 的模板
# 模板语法: ${SPEC_ID} / ${TIMESTAMP} / ${INTENT}
# 注意: meta_template 级别的 spec 不做从属链验证

spec:
  id: "designer_intent_template"
  version: "1.0"
  level: "meta_template"
  name: "designer_intent_template"
  parent: null
  status: draft
  created_at: "2026-01-01T00:00:00Z"

content:
  description: "L1 Intent/Goal spec 生成模板"

template: |
  # Intent Spec ${SPEC_ID}
  # Generated by VibeX Agent at ${TIMESTAMP}

  spec:
    id: "${SPEC_ID}"
    type: goal
    input: "${INTENT}"
    output: ""
    boundary: ""
    behavior: ""
    status: draft
    created_at: "${TIMESTAMP}"
    parent: null

  notes:
    - Auto-generated draft. Await user confirmation of intent.
    - Use spec_validate to check YAML syntax.
    - Use spec_feature to break into features once confirmed.
