# ============================================================
# Level 5d — DSL Canvas 测试规格
# parent: dsl-canvas (L4)
# ============================================================
spec:
  version: "1.0"
  level: "5d_test"
  name: "dsl-canvas_test"
  parent: "dsl-canvas"
  status: "active"

content:
  test_cases:
    - id: "TC-001"
      name: "从 5 个 specs 生成 Mermaid 图"
      input: "[L1, L2, L3, L4, L5] specs with parent chain"
      expected: "valid mermaid flowchart code"

    - id: "TC-002"
      name: "层级颜色正确"
      input: "spec with level=3"
      expected: "node color=#eab308 (yellow)"

    - id: "TC-003"
      name: "点击节点触发 spec.opened"
      input: "click on L4 node"
      expected: "event spec.opened fired with correct specId"
