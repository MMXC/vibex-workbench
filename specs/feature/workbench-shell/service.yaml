---
spec:
  version: "0.1.0"
  level: "5a_service"
  name: "workbench-shell-service"
  parent: "workbench-shell-feature"
  status: "contract"
meta:
  owner: "shared"
  tech_stack: ["contract"]
  description: "Workbench Shell 接口契约 — frontend 和 backend 共用"
content:
  public_api:
    - method: "toggleLeftSidebar"
      signature: "toggleLeftSidebar(): void"
    - method: "toggleRightSidebar"
      signature: "toggleRightSidebar(): void"
    - method: "setActivePanel"
      signature: "setActivePanel(panelId: PanelId): void"
    - method: "toggleComposer"
      signature: "toggleComposer(): void"
    - method: "setTheme"
      signature: "setTheme(theme: 'light'|'dark'|'system'): void"
    - method: "openCommandPalette"
      signature: "openCommandPalette(): void"
    - method: "closeCommandPalette"
      signature: "closeCommandPalette(): void"
  notes:
    - "workbench-shell 是纯 UI 组件，不发 SSE 事件"
    - "状态存储在前端 uiStore 中，无跨进程通信需求"
  data_contract:
    types: ["PanelId", "UIState", "ModalInstance", "DragState"]
