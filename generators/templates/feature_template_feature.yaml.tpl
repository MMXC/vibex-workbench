# Feature Spec ${FEATURE_ID}
# Parent: ${PARENT_ID}
# Generated: ${TIMESTAMP}

spec:
  id: "${FEATURE_ID}"
  version: "1.0"
  level: "4_feature"
  name: "${SAFE_NAME}"
  parent: "${PARENT_ID}"
  status: draft
  created_at: "${TIMESTAMP}"

content:
  description: "${FEATURE_NAME}"
  sub_specs:
    uiux: "${SAFE_NAME}_uiux.yaml"
